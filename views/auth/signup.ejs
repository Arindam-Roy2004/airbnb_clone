<%- include('../partials/head') %>
  </head>

  <body class="bg-gray-100">
    <%- include('../partials/nav') %>

      <main class="container mx-auto flex items-center justify-center min-h-[calc(100vh-80px)] py-4">
        <div class="bg-white rounded-xl shadow-xl border border-gray-200 p-8 w-full max-w-xl">
          <!-- Header (more compact) -->
          <div class="text-center mb-6 border-b border-gray-200 pb-4">
             <h1 class="text-lg font-bold text-gray-800">Sign up</h1>
          </div>

          <div class="mb-6">
             <h2 class="text-2xl font-semibold text-gray-800 mb-2">Create your profile</h2>
             <p class="text-gray-500 text-sm">Join StayEase community of hosts and guests.</p>
          </div>

          <!-- Signup Form (compact spacing) -->
          <form action="/signup" method="POST" class="space-y-4">
            
              <% if(typeof errors !=='undefined' && errors.length> 0){ %>
                <div class="bg-red-50 border border-red-200 text-red-600 px-4 py-3 rounded-lg text-sm mb-4">
                  <ul class="list-disc list-inside">
                    <% errors.forEach(function(error) { %>
                      <li><%= error%></li>
                    <% }); %>
                  </ul>
                </div>
              <% } %>

              <div class="border border-gray-400 rounded-lg overflow-hidden focus-within:border-black focus-within:ring-1 focus-within:ring-black">
                  <!-- First Name -->
                  <div class="px-3 pt-2 border-b border-gray-400">
                     <label for="firstName" class="block text-xs text-gray-500">First Name</label>
                     <input
                       type="text"
                       id="firstName"
                       name="firstName"
                       placeholder="First name"
                       class="w-full border-none p-0 text-gray-900 placeholder-gray-400 focus:ring-0 sm:text-sm pb-2 outline-none"
                       required
                     />
                  </div>
                  <!-- Last Name -->
                  <div class="px-3 pt-2 border-b border-gray-400">
                     <label for="lastName" class="block text-xs text-gray-500">Last Name</label>
                     <input
                       type="text"
                       id="lastName"
                       name="lastName"
                       placeholder="Last name"
                       class="w-full border-none p-0 text-gray-900 placeholder-gray-400 focus:ring-0 sm:text-sm pb-2 outline-none"
                       required
                     />
                  </div>
                  <!-- Email -->
                  <div class="px-3 pt-2 border-b border-gray-400">
                     <label for="email" class="block text-xs text-gray-500">Email</label>
                     <input
                       type="email"
                       id="email"
                       name="email"
                       placeholder="Email address"
                       class="w-full border-none p-0 text-gray-900 placeholder-gray-400 focus:ring-0 sm:text-sm pb-2 outline-none"
                       required
                     />
                  </div>
                  <!-- Password -->
                  <div class="px-3 pt-2 border-b border-gray-400">
                     <label for="password" class="block text-xs text-gray-500">Password</label>
                     <input
                       type="password"
                       id="password"
                       name="password"
                       placeholder="Password"
                       class="w-full border-none p-0 text-gray-900 placeholder-gray-400 focus:ring-0 sm:text-sm pb-2 outline-none"
                       required
                     />
                  </div>
                   <!-- Confirm Password -->
                   <div class="px-3 pt-2">
                     <label for="confirmPassword" class="block text-xs text-gray-500">Confirm Password</label>
                     <input
                       type="password"
                       id="confirmPassword"
                       name="confirmPassword"
                       placeholder="Confirm password"
                       class="w-full border-none p-0 text-gray-900 placeholder-gray-400 focus:ring-0 sm:text-sm pb-2 outline-none"
                       required
                     />
                  </div>
              </div>

              <p class="text-xs text-gray-500 mt-2">
                 By selecting <strong>Agree and continue</strong>, I agree to StayEase's <a href="#" class="underline text-blue-600">Terms of Service</a>, <a href="#" class="underline text-blue-600">Payments Terms of Service</a>, and <a href="#" class="underline text-blue-600">Nondiscrimination Policy</a> and acknowledge the <a href="#" class="underline text-blue-600">Privacy Policy</a>.
              </p>

              <button
                type="submit"
                class="w-full flex justify-center py-3 px-4 border border-transparent rounded-lg shadow-sm text-lg font-bold text-white bg-airbnb hover:bg-airbnb-dark focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-red-500 transition"
              >
                Agree and continue
              </button>
          </form>
          
          <div class="mt-6 text-center">
             <p class="text-sm text-gray-600">Already have an account? <a href="/login" class="font-semibold text-black underline">Log in</a></p>
          </div>
        </div>
      </main>
  </body>

  </html>